#include <stdio.h>
#include <stdlib.h>
#include <string.h>
 int main(){
/*typedef struct
{ int nbre;
    char Lettre;
}occurence;
typedef occurence tab[26]={{1,'a'},{1,'b'} };

int main()
{ int *t , i,n=4;
 for(i=0;i<n;i++)
 {
     printf("donner lele %d ",i+i);
     scanf("%d",t+i);
 }
    for(i=0;i<n;i++)
        printf("%d  ",*(t+i));*/

/*char nom[20],nomM[20]; int i=0;
printf("donner la chaine :");
scanf("%s",nom);
while(nom[i]!='\0')
{nomM[i]=toupper(nom[i]);
i++;
}
printf("\n %s ",nomM);*/


    return 0;
}
